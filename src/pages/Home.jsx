// eslint-disable-next-line no-unused-vars
import React from 'react';
import Title from './../components/Title';

const Home = () => {
    return (
        <div>
            <Title text="Nada interesante por aquí, mejor vete a Characters"/>
        </div>
    );
};

export default Home;